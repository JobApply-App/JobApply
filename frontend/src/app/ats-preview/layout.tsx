import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'ATS Preview' }

export default function AtsPreviewLayout({ children }: { children: React.ReactNode }) {
  return children
}
